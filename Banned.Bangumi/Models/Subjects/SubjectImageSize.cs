namespace Banned.Bangumi.Models.Subjects;

/// <summary>
/// 指定条目图片尺寸。<br/>
/// Specifies a subject image size.
/// </summary>
public enum SubjectImageSize
{
    /// <summary>小尺寸。 / Small size.</summary>
    Small,

    /// <summary>网格尺寸。 / Grid size.</summary>
    Grid,

    /// <summary>大尺寸。 / Large size.</summary>
    Large,

    /// <summary>中等尺寸。 / Medium size.</summary>
    Medium,

    /// <summary>常用尺寸。 / Common size.</summary>
    Common,
}
